<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="10dp">

    <EditText
        android:id="@+id/recycler_item_ingredient_quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:textSize="16sp"
        android:text="@string/detail_edit_recycler_item_ingredient_quantity_default"
        android:textColor="@color/colorPrimary"
        android:background="@color/windowBackground"
        android:inputType="text"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/recycler_item_ingredient_add_to_shopping_list_button"
        android:layout_toEndOf="@+id/recycler_item_ingredient_add_to_shopping_list_button"/>

    <EditText
        android:id="@+id/recycler_item_ingredient_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/textColorPrimary"
        android:textSize="16sp"
        android:text="@string/detail_edit_recycler_item_ingredient_name_default"
        android:inputType="textMultiLine"
        android:background="@color/windowBackground"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/recycler_item_ingredient_quantity"
        android:layout_toEndOf="@+id/recycler_item_ingredient_quantity"/>

    <ImageButton
        android:id="@+id/recycler_item_ingredient_add_to_shopping_list_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_add_circle_filled_green_24dp"
        android:background="@color/windowBackground"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"/>

    <ImageButton
        android:id="@+id/recycler_item_ingredient_delete_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:src="@drawable/ic_delete_red_24dp"
        android:background="@color/windowBackground"
        android:layout_centerVertical="true"
        android:visibility="gone"/>

</RelativeLayout>

